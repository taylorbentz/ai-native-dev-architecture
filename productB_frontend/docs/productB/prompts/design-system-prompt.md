# Prompt: Align a Component to the Design System

> Refactor `src/components/<Name>.tsx` to use the shared design tokens (spacing, color, typography)
> instead of hard-coded values. Preserve behavior and props. Confirm the accessibility baseline still
> holds (keyboard focus, labels, contrast). Run `/component-new`'s checks: `npm test && npm run lint`.
