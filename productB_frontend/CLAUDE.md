# productB_frontend

ProductB's web UI (React + TypeScript). Owns its own assistant capabilities in `.claude/`.

- **Build/verify:** `npm test && npm run lint`
- **Branch model:** trunk
- **Layout:** one component per file under `src/components/`; co-locate styles and tests.
- **Style:** 2-space indent; components are typed function components.
