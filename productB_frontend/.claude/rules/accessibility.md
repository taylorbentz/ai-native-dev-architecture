---
paths:
  - "**/*.tsx"
---

# Accessibility Baseline

- Every interactive element is reachable and operable by keyboard.
- Images have meaningful alt text; decorative images use empty alt.
- Form controls have associated labels; don't rely on placeholder text as a label.
- Color is never the only signal; maintain sufficient contrast.
- Use semantic elements before reaching for ARIA roles.
