# Generated per-developer, never committed
**/.mcp.json
**/.claude/settings.local.json

# Shared capability is installed as a versioned plugin, not copied/linked per repo —
# so there are no composed-in files to ignore here anymore.

# Tool-server virtual environments
**/.venv/
**/__pycache__/

# Dependencies / build output
**/node_modules/
**/dist/
**/build/
**/.terraform/
*.tfstate
*.tfstate.*

# Editor / OS noise
.DS_Store
.idea/
.vscode/
