---
paths:
  - "**/*.ext"
---

# Rule Title

Keep rules under ~100 lines. Prefer several narrow, path-scoped rules over one broad rule, so the
assistant only loads guidance relevant to the files in play.

- Concrete instruction one.
- Concrete instruction two.
