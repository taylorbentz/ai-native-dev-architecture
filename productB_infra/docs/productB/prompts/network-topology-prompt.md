# Prompt: Review Network Topology

> Review the network module under `modules/`. Confirm subnets are sized for expected growth, no resource
> is publicly reachable unless intended, and every ingress rule has a comment explaining why it exists.
> Produce a `terraform plan` and confirm there are no unintended destroy/replace operations.
